// Fill in with your Phrase (PhraseApp Strings) project credentials.
// Get these from: Phrase > Project settings > In-Context Editor
window.PHRASEAPP_CONFIG = {
  projectId: "YOUR_PHRASE_PROJECT_ID",
  // Optional: set to a real prefix/datacenter if your org uses one.
  // prefix: "https://app.phrase.com/",

  // Toggle via ?hidingClasses=off in the URL — see index.html.
  // Default vendor value: ["hidden", "visuallyhidden", "invisible"]
  hidingClasses: ["hidden", "visuallyhidden", "invisible"],
};
