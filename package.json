{
  "name": "phrase-ice-tailwind-repro",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal repro: Phrase In-Context Editor skips markers inside Tailwind `hidden <bp>:block` elements even when visible at the current viewport",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "watch:css": "tailwindcss -i ./src/input.css -o ./styles.css --watch",
    "serve": "npx --yes serve -p 4000 ."
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "concurrently": "10.0.4",
    "tailwindcss": "4.3.3"
  }
}
